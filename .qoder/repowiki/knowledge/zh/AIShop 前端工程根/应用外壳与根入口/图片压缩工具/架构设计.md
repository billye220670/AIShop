单一职责的纯函数模块，仅暴露 `compressImageFile` 一个导出接口；内部通过三个私有辅助函数（`readAsDataURL`、`loadImage`、`canvasToBlob`）封装 FileReader、HTMLImageElement 与 Canvas API 的异步调用。处理流程为：跳过 gif/svg → 读取 DataURL → 解码为 Image → 计算缩放比例 → 绘制到 canvas 并导出 Blob → 必要时二次降质量 → 转回 DataURL 返回。所有异常路径均回退到原图，保证不阻塞用户发送。