npm run dev 启动监听 0.0.0.0 的 Vite 开发服务器；npm run build 先执行 tsc -b 再 vite build；npm run lint 运行 ESLint；npm run preview 预览生产构建产物；Electron 模式使用 npm run dev:electron / build:electron / preview:electron；Windows 桌面打包通过 npm run package:win（或带 -SkipBuild 跳过预构建）。