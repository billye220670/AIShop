# AI 回复停止行为优化

## 更改内容

### 1. 类型定义更新 (`src/types/index.ts`)

为 `Message` 和 `MessageVersion` 接口添加了新字段：
- `stoppedByUser?: boolean` - 标识消息是否被用户停止

### 2. 停止逻辑优化 (`src/hooks/useChat.ts`)

#### `stopGeneration` 函数
- **原逻辑**: 无论 AI 是否已回答内容，都会清空并替换为"用户已取消生成"
- **新逻辑**: 
  - 如果 AI 已经有内容（content 不为空），保留已有内容，设置 `stoppedByUser: true`
  - 如果 AI 还没有内容（content 为空），保留空内容，设置 `stoppedByUser: true`
  - 同时处理普通消息和多版本消息

#### `sendMessage` 错误处理
- 更新 AbortError 的处理逻辑，与 `stopGeneration` 保持一致

#### `regenerateMessage` 和 `compareWithModel` 错误处理
- 更新这两个函数中的 AbortError 处理，应用相同的停止逻辑

### 3. UI 渲染更新 (`src/components/chat/MessageBubble.tsx`)

#### 新增停止状态显示
在消息内容后添加停止状态的 UI：

**情况 1: AI 已经回答了部分内容**
- 保留已回答的内容
- 在内容下方显示分割线，中间显示"用户已停止"文字

**情况 2: AI 还没返回内容（等待动画中）**
- 显示一个提示卡片：
  - 图标 + "请求已被取消。"文字
  - "立即重新生成"按钮（点击调用 `onRegenerate`）

## 实现效果

### 场景 1: AI 已回答内容时点击停止
```
[AI 已回答的内容...]

────── 用户已停止 ──────

[正常的操作按钮: 复制、保存、重新生成、引用]
```

### 场景 2: AI 未回答内容时点击停止
```
┌─────────────────────────┐
│ ⓘ 请求已被取消。         │
│                         │
│ ┌───────────────────┐   │
│ │ 立即重新生成       │   │
│ └───────────────────┘   │
└─────────────────────────┘
```

## 技术细节

1. **内容判断**: 使用 `content.trim().length > 0` 来判断是否有实际内容
2. **版本兼容**: 同时处理普通消息和多版本消息（用于多模型比较功能）
3. **状态同步**: 在 `stopGeneration`、`sendMessage`、`regenerateMessage`、`compareWithModel` 中统一处理
4. **向下兼容**: 新增的 `stoppedByUser` 字段为可选，不影响现有数据

## 测试建议

1. 测试场景 1: 发送问题后，等待 AI 开始回答（有文字出现），然后点击停止按钮
   - 验证: 已回答的内容保留，显示"用户已停止"分割线
   
2. 测试场景 2: 发送问题后，在等待动画（三个点）阶段立即点击停止按钮
   - 验证: 显示"请求已被取消"卡片和"立即重新生成"按钮
   
3. 测试场景 3: 点击"立即重新生成"按钮
   - 验证: 重新发起请求，正常显示新的回答

4. 测试多版本场景: 在多模型比较功能中测试停止行为
   - 验证: 各版本的停止状态独立管理，不相互影响
